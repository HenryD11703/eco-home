version: "3.9"

services:
  mongo:
    image: mongo:latest
    container_name: mongo_eco
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
